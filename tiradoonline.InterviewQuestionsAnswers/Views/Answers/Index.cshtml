@model IEnumerable<tiradoonline.DataAccess.InterviewQuestionsAnswers.Models.Answers>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Questions.Question)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Answer)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.create_dt)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Questions.Question)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Answer)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.create_dt)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnswerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnswerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnswerID })
        </td>
    </tr>
}

</table>
